# 出力画像・動画
output/
*.png
*.mp4

# Secrets（絶対にコミットしない）
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Icon?
Icon
._*

# ── 作業資料（★案A / 2026-08-24）─────────────────────────────
# 依頼文・引き継ぎ・設計書・手順書は実物件名を含みうる。リポには入れない。
# 経緯: 8/24 に未追跡50件が git add -A の射程に入っていた（croco-bridge は AUTO_COMMIT=1）。
# ★列挙ではなく「原則無視・許可制」にしてある。命名規則から外れた資料を
#   取りこぼさないため（実際 *_依頼文_*.md だけでは4件が漏れた）。
*.md
!README.md
!AI物件シミュレーション_SNS投稿_運用ルール_v1.0_20260701.md
# 資料を1件だけ追跡したくなったら、上と同じ形で ! を足す（既に追跡済みの .md は影響を受けない）

# スキャン用の実棟名リスト（★絶対にコミットしない。通常はリポ外に置く）
_実棟名リスト_*.txt

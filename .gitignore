# 出力画像・動画
output/
*.png
*.mp4

# Secrets（絶対にコミットしない）
.streamlit/secrets.toml

# Python
__pycache__/
*.pyc
.venv/
venv/

# OS
.DS_Store
Icon?
Icon
._*

# ── 作業資料（★案A / 2026-08-24）─────────────────────────────
# 依頼文・引き継ぎ・設計書・手順書は実物件名を含みうる。リポには入れない。
# 経緯: 8/24 に未追跡50件が git add -A の射程に入っていた（croco-bridge は AUTO_COMMIT=1）。
# ★列挙ではなく「原則無視・許可制」にしてある。命名規則から外れた資料を
#   取りこぼさないため（実際 *_依頼文_*.md だけでは4件が漏れた）。
*.md
!README.md
!AI物件シミュレーション_SNS投稿_運用ルール_v1.0_20260701.md
# 資料を1件だけ追跡したくなったら、上と同じ形で ! を足す（既に追跡済みの .md は影響を受けない）

# スキャン用の実棟名リスト（★絶対にコミットしない。通常はリポ外に置く）
_実棟名リスト_*.txt

# ★A/B のリール生成 CLI（2026-08-26 作成・未追跡のまま5日経っていた）
#   このリポは Public。add -A に巻き込まれると実名コメント込みで履歴に入るため除外する。
#   name_guard は生きているが、名簿の行と完全一致しないと当たらない。棟名だけの略記は素通りする（2026-09-03 実測）。
#   ★除外を恒久の結論にしない。扱いは A/B 終了後（2026-09-11 以降）に谷合が決める。
make_reel.py
reel_config.py
pair_check.py
make_caption.py
